{
  "name": "swap-meet-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --target=es2020 --outfile=dist/bundle.js",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.ts",
    "verify": "npm run check && npm test && npm run build"
  },
  "devDependencies": {
    "esbuild": "0.27.2",
    "typescript": "^5.9.0"
  }
}
